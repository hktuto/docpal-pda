# @warehouse/android — DEPRECATED

> **Deprecated (2026-07).** This native Kotlin + Compose + Room app was a
> proof-of-concept for the offline-first rewrite. The product direction is the
> Capacitor web client (`apps/web`) talking to the new backend
> (`apps/backend`, see `docs/backend/`). Do not add features here; the code
> stays in the repo as reference only (its scan-parsing and flow logic informed
> the backend API design).
>
> The build/test commands below are kept for archaeology, not for ongoing work.
