diff --git a/android/build.gradle b/android/build.gradle
index 3924fc24e940e02e7e0eb2135ce07547b69a40a6..18b5e6942440fa44e8f1607f7110e7ee74145652 100644
--- a/android/build.gradle
+++ b/android/build.gradle
@@ -5,7 +5,7 @@ ext {
     androidxAppCompatVersion = project.hasProperty('androidxAppCompatVersion') ? rootProject.ext.androidxAppCompatVersion : '1.7.1'
     androidxJunitVersion = project.hasProperty('androidxJunitVersion') ? rootProject.ext.androidxJunitVersion : '1.3.0'
     androidxEspressoCoreVersion = project.hasProperty('androidxEspressoCoreVersion') ? rootProject.ext.androidxEspressoCoreVersion : '3.7.0'
-    mlkitTextRecognitionVersion = project.hasProperty('mlkitTextRecognitionVersion') ? rootProject.ext.mlkitTextRecognitionVersion : '19.0.1'
+    mlkitTextRecognitionVersion = project.hasProperty('mlkitTextRecognitionVersion') ? rootProject.ext.mlkitTextRecognitionVersion : '16.0.1'
 }
 
 buildscript {
@@ -67,6 +67,6 @@ dependencies {
     testImplementation "junit:junit:$junitVersion"
     androidTestImplementation "androidx.test.ext:junit:$androidxJunitVersion"
     androidTestImplementation "androidx.test.espresso:espresso-core:$androidxEspressoCoreVersion"
-    implementation "com.google.android.gms:play-services-mlkit-text-recognition:$mlkitTextRecognitionVersion"
+    implementation "com.google.mlkit:text-recognition:$mlkitTextRecognitionVersion"
     implementation "org.jetbrains.kotlin:kotlin-stdlib:$kotlin_version"
 }
diff --git a/android/src/main/java/com/pantrist/ml/CapacitorPluginMlKitTextRecognition.kt b/android/src/main/java/com/pantrist/ml/CapacitorPluginMlKitTextRecognition.kt
index 066c5d5d4c23e276cd7c73273bf00da28fd69b74..727e5f58ccdf02265a0447fd0fa1f7f69db71e9d 100644
--- a/android/src/main/java/com/pantrist/ml/CapacitorPluginMlKitTextRecognition.kt
+++ b/android/src/main/java/com/pantrist/ml/CapacitorPluginMlKitTextRecognition.kt
@@ -1,6 +1,7 @@
 package com.pantrist.ml
 
 import android.util.Base64
+import android.util.Log
 import com.getcapacitor.*
 import com.getcapacitor.annotation.CapacitorPlugin
 import java.io.IOException
@@ -33,6 +34,7 @@ class CapacitorPluginMlKitTextRecognition : Plugin() {
         call.reject("Decoded image is null")
         return
       }
+      Log.d("MLKitOCR", "Decoded bitmap: ${decodedByte.width}x${decodedByte.height}")
       image = InputImage.fromBitmap(decodedByte, rotation)
     } catch (e: IOException) {
       call.reject("Unable to parse image")
@@ -80,6 +82,7 @@ class CapacitorPluginMlKitTextRecognition : Plugin() {
         call.resolve(ret)
       }
       .addOnFailureListener { e ->
+        Log.e("MLKitOCR", "Unable process image", e)
         call.reject("Unable process image!", e)
       }
   }
